includes:
- ./text.yaml
- configs/datasets/memes/bert.yaml
- configs/models/unimodal/bert.yaml

model_config:
  unimodal_text:
    classifier:
      num_labels: 3
      type: mlp
      params:
        in_dim: 768
        num_layers: 2

training:
  batch_size: 16
